<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.quinielaapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".modules.sellerAdminModules.usersDetails.views.UsersDetailActivity">
        </activity>
        <activity android:name=".modules.sellerAdminModules.searchUsers.views.SearchUsersActivity">
        </activity>
        <activity android:name=".modules.logIn.views.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".modules.signIn.SignUpActivity">
        </activity>
        <activity android:name=".DashboardActivity">
        </activity>
        <activity android:name=".SplashActivity">
        </activity>
    </application>

</manifest>